# Core dependencies
opencv-python>=4.8.0  # For camera feed processing and image manipulation
numpy>=1.24.0        # Required by OpenCV and for array operations

# GUI dependencies - note: PyGObject (gi) needs to be installed via system package manager
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-gtk-4.0 libadwaita-1-dev
# These are listed here for documentation but cannot be installed via pip:
# - PyGObject (gi)
# - GTK4
# - libadwaita-1 